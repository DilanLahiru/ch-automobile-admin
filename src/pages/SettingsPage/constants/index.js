/**
 * Settings Page Constants
 * Centralized configuration for validation rules and pagination
 */

export const VALIDATION_RULES = {
  MIN_NAME_LENGTH: 2,
  MIN_PASSWORD_LENGTH: 6,
};

export const PAGINATION_CONFIG = {
  ITEMS_PER_PAGE: 10,
  VISIBLE_PAGES: 5,
};
